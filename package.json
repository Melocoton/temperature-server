{
  "name": "temperature-server",
  "version": "1.0.0",
  "description": "MQTT NodeMCU temperature server",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "run": "node dist/main.js",
    "build-run": "tsc && node dist/main.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
